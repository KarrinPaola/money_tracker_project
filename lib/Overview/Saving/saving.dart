import 'package:flutter/material.dart';

class OverviewSaving extends StatelessWidget {
  const OverviewSaving({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}