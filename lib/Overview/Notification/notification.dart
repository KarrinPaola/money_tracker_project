import 'package:flutter/material.dart';

class OverviewNotification extends StatelessWidget {
  const OverviewNotification({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}